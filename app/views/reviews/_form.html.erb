<div>
  <%= render'shared/errors', obj: @review%>
  <%= form_with model: @review, local: false do |f| %>
    <div class="container, id="review_add">
      <p><%= f.label :body %></p>
      <p><%= f.text_area :user_review %></p>
    </div>
      <%= f.hidden_field :product_id %>
    <div>
      <%= f.submit %>
    </div>
  <% end %>
</div>
