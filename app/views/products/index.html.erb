<h1>index</h1>

<% @products.each do |product| %>
  <p><%= product.title %></p>
  <p><%= product.description%></p>
  <p><%= image_tag(product.image) %></p>
  <p><%=link_to 'Detail', product_path(product) %></p>
<% end %>
