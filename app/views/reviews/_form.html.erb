<div>
  <%= form_with model: @review, local: true do |f| %>
    <div>
      <p><%= f.label :body %></p>
      <p><%= f.text_field :user_review %></p>
    </div>
      <%= f.hidden_field :product_id %>
    <div>
      <%= f.submit %>
    </div>
  <% end %>
</div>
